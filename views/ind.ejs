<!DOCTYPE html>
<html>
<style>

label {
    font: normal 17px courier !important;
    color:#6699ff;
}
form{
  padding:260px;
  height:100%;
}

.navbar {
  overflow: hidden;
  background-color: 	#e5e5e5;
  position: fixed;
  top: 0;
  width: 100%;
  height:15%;
}


.navbarr {
  overflow: hidden;
  background-color:		#e5e5e5;
  position: fixed;
  left: 0;
  width: 15%;
  height:100%;
}
.btn-group button {
  background-color: #6699ff; /* Green background */
  border: 1px ; /* Green border */
  color: white; /* White text */
  padding: 10px 24px; /* Some padding */
  cursor: pointer; /* Pointer/hand icon */
  width:70%; /* Set a width if needed */
  display: block; /* Make the buttons appear below each other */
 
 
}

.btn-group button:not(:last-child) {
  border-bottom: none; /* Prevent double borders */
}

/* Add a background color on hover */
.btn-group button:hover {
  background-color: 	#0000FF;
}


input[type=text], select {
  width: 100%;
  padding: 12px 20px;
  margin: 8px 0;
  display: inline-block;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box;
}

input[type=text] {
  width: 100%;
  padding: 12px 20px;
  margin: 8px 0;
  display: inline-block;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box;
}

input[type=password] {
  width: 100%;
  padding: 12px 20px;
  margin: 8px 0;
  display: inline-block;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box;
}


input[type=submit] {
  width: 100%;
  background-color: #6699ff;
  color: white;
  padding: 14px 20px;
  margin: 8px 0;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}
h1{
    text-align:center;
}
 

/* input[type=submit]:hover {
  background-color: #45a049;
} */

/* div {
  border-radius: 5px;
  background-color: #f2f2f2;
  padding: 20px;
} */
</style>
  <head>
    <title>MAP</title>
  <!--   <link rel='stylesheet' href='/stylesheets/style.css' /> -->
  </head>
  <body>

      <h1><%= vall %></h1>

    <form id ="contactForm" action="/api/get" method="get">
       <fieldset> 
            <label for="name">UserName:</label>
            <input type="text" id="name" name="name" placeholder="Enter your UserName" size="80" width="100"/>
        </br></br> </br></br>
            <label for="password">Password:</label>
            <input type="text" id="password" name="password" placeholder="Enter Password" size="81.5" width="100"/>
          </br></br></br></br>
         <!--    <label for="xmppId">XMPP ID:</label>
            <input  id="xmpp" name="xmpp" placeholder="Enter your XMPP ID"></input>
          </br></br>
            <label for="number">RC NUMBER:</label>
            <input id="number" name = "number" placeholder="Enter your RC Number"></input>
          </br></br> -->
            <input type="submit" value="GET MAPPINGS" id="sub" />
       </fieldset> 
    </form>
   
    </div>

    <div id="result"></div>
 <!--    <script type="text/javascript">

    window.onload = function() {

      // Check for LocalStorage support.
      if (localStorage) {
    
        // Add an event listener for form submissions
        document.getElementById('contactForm').addEventListener('submit', function() {
          // Get the value of the name field.
          var name = document.getElementById('name').value;
          var paaas =  document.getElementById('password').value;
    
          // Save the name in localStorage.
          session.setItem('name', name);
          session.setItem('paaas', paaas);
        });
    
      }
    
    }
  </script> -->
  </body>
</html>

















<!-- /* 
imp sessiom -->

<!-- script type='text/javascript'>
  $(document).ready(function() {
     //option A
     $("form").submit(function(e){
         alert('submit intercepted');
         e.preventDefault(e);
     });
 });

 </script>  


<script>
  if (typeof(Storage) !== "undefined") {
      // Store
      var name = document.getElementById('name');
      var naaam = "dodhiya";
      localStorage.setItem("lastname2", naaam);
      // Retrieve
      document.getElementById("result").innerHTML = localStorage.getItem("lastname");
    } else {
      document.getElementById("result").innerHTML = "Sorry, your browser does not support Web Storage...";
    }</script> */



 -->











